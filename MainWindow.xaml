<Window x:Class="_6週目_MYAT_THADAR_LINN_Vicroty_Fall.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_6週目_MYAT_THADAR_LINN_Vicroty_Fall"
        mc:Ignorable="d"
        Title="6周目_Victory fall_ WPF_Base" 
        Height="720" Width="400" Background="Black" 
        WindowStyle="None" AllowsTransparency="True"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style TargetType="Button" x:Key="NeonButtonStyle">
            <Setter Property="Background" Value="#101010"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#80FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0"
                                                  Color="White" BlurRadius="15" Opacity="0.8"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="White"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="#FFC0F0FF" BlurRadius="25" Opacity="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter Property="Background" Value="#303030"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="EqualButtonStyle" BasedOn="{StaticResource NeonButtonStyle}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" 
                                                  Color="#FF808080" 
                                                  BlurRadius="10" Opacity="0.6"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="GlobeButtonStyle" BasedOn="{StaticResource NeonButtonStyle}">
            <Setter Property="Content" Value="&#x1F310;"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol, Segoe UI Emoji"/>
            <Setter Property="FontSize" Value="24"/>
        </Style>
    </Window.Resources>

    <Border Background="#050505"
            CornerRadius="20"
            BorderBrush="#40FFFFFF" 
            BorderThickness="2">

        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="White" BlurRadius="30" Opacity="0.8"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="Black" 
                    BorderBrush="White" 
                    BorderThickness="1" 
                    CornerRadius="10" 
                    Margin="0,0,0,10">

                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="White" BlurRadius="20" Opacity="0.7"/>
                </Border.Effect>

                <StackPanel Margin="10">
                    <TextBlock x:Name="ModeTextBlock" 
                               Text="Mode: English" 
                               Foreground="White" 
                               FontSize="14" 
                               HorizontalAlignment="Left" 
                               VerticalAlignment="Top"/>
                    <TextBlock x:Name="DisplayTextBlock" 
                               Text="0" 
                               Foreground="#FFC0F0FF" 
                               FontSize="30" 
                               FontWeight="Light"
                               HorizontalAlignment="Right" 
                               VerticalAlignment="Bottom" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="0" Grid.Column="0" Content="MC" Style="{StaticResource NeonButtonStyle}" x:Name="BtnMC"/>
                <Button Grid.Row="0" Grid.Column="1" Content="MR" Style="{StaticResource NeonButtonStyle}" x:Name="BtnMR"/>
                <Button Grid.Row="0" Grid.Column="2" Content="M+" Style="{StaticResource NeonButtonStyle}" x:Name="BtnMPlus"/>
                <Button Grid.Row="0" Grid.Column="3" Content="M-" Style="{StaticResource NeonButtonStyle}" x:Name="BtnMMinus"/>

                <Button Grid.Row="1" Grid.Column="0" Content="AC" Style="{StaticResource NeonButtonStyle}" x:Name="BtnAC"/>
                <Button Grid.Row="1" Grid.Column="1" Content="DEL" Style="{StaticResource NeonButtonStyle}" x:Name="BtnDEL"/>
                <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource GlobeButtonStyle}" x:Name="BtnGlobe"/>
                <Button Grid.Row="1" Grid.Column="3" Content="%" Style="{StaticResource NeonButtonStyle}" x:Name="BtnPercent"/>

                <Button Grid.Row="2" Grid.Column="0" Content="7" Style="{StaticResource NeonButtonStyle}" x:Name="Btn7"/>
                <Button Grid.Row="2" Grid.Column="1" Content="8" Style="{StaticResource NeonButtonStyle}" x:Name="Btn8"/>
                <Button Grid.Row="2" Grid.Column="2" Content="9" Style="{StaticResource NeonButtonStyle}" x:Name="Btn9"/>
                <Button Grid.Row="2" Grid.Column="3" Content="÷" Style="{StaticResource NeonButtonStyle}" x:Name="BtnDivide"/>

                <Button Grid.Row="3" Grid.Column="0" Content="4" Style="{StaticResource NeonButtonStyle}" x:Name="Btn4"/>
                <Button Grid.Row="3" Grid.Column="1" Content="5" Style="{StaticResource NeonButtonStyle}" x:Name="Btn5"/>
                <Button Grid.Row="3" Grid.Column="2" Content="6" Style="{StaticResource NeonButtonStyle}" x:Name="Btn6"/>
                <Button Grid.Row="3" Grid.Column="3" Content="X" Style="{StaticResource NeonButtonStyle}" x:Name="BtnMultiply"/>

                <Button Grid.Row="4" Grid.Column="0" Content="1" Style="{StaticResource NeonButtonStyle}" x:Name="Btn1"/>
                <Button Grid.Row="4" Grid.Column="1" Content="2" Style="{StaticResource NeonButtonStyle}" x:Name="Btn2"/>
                <Button Grid.Row="4" Grid.Column="2" Content="3" Style="{StaticResource NeonButtonStyle}" x:Name="Btn3"/>
                <Button Grid.Row="4" Grid.Column="3" Content="-" Style="{StaticResource NeonButtonStyle}" x:Name="BtnSubtract"/>

                <Button Grid.Row="5" Grid.Column="0" Content="0" Style="{StaticResource NeonButtonStyle}" x:Name="Btn0"/>
                <Button Grid.Row="5" Grid.Column="1" Content="." Style="{StaticResource NeonButtonStyle}" x:Name="BtnDecimal"/>
                <Button Grid.Row="5" Grid.Column="2" Content="+" Style="{StaticResource NeonButtonStyle}" x:Name="BtnAdd"/>
                <Button Grid.Row="5" Grid.Column="3" Content="=" Style="{StaticResource EqualButtonStyle}" x:Name="BtnEqual"/>
            </Grid>
        </Grid>
    </Border>
</Window>